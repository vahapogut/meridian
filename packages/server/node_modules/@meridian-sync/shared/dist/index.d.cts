/**
 * Meridian — Hybrid Logical Clock (HLC)
 *
 * Combines wall-clock time with a logical counter to provide:
 * - Monotonicity: HLC never goes backward even if system clock adjusts
 * - Causality: send/recv operations preserve happens-before ordering
 * - Compactness: Serializes to "wallTime-counter-nodeId" string
 *
 * Based on the paper: "Logical Physical Clocks and Consistent Snapshots
 * in Globally Distributed Databases" by Kulkarni et al.
 */
interface HLCTimestamp {
    /** Wall-clock time in milliseconds */
    wallTime: number;
    /** Logical counter for ordering events within the same millisecond */
    counter: number;
    /** Unique identifier for the node/client that generated this timestamp */
    nodeId: string;
}
/**
 * Hybrid Logical Clock implementation.
 *
 * Usage:
 * ```ts
 * const clock = new HLC('client-abc');
 * const ts = clock.now();          // Generate timestamp for local event
 * const sent = clock.send();       // Generate timestamp for outgoing message
 * clock.recv(remoteTimestamp);      // Update clock with received timestamp
 * ```
 */
declare class HLC {
    private _wallTime;
    private _counter;
    private readonly _nodeId;
    constructor(nodeId: string, initialTime?: number);
    /** Current node ID */
    get nodeId(): string;
    /**
     * Generate a timestamp for a local event.
     * Ensures monotonicity even if the system clock goes backward.
     */
    now(): HLCTimestamp;
    /**
     * Generate a timestamp for an outgoing message.
     * Equivalent to `now()` but semantically indicates a send event.
     */
    send(): HLCTimestamp;
    /**
     * Update the local clock upon receiving a remote timestamp.
     * Merges the remote time with local time to maintain causal ordering.
     *
     * @param remote - The timestamp received from another node
     * @returns The updated local timestamp
     */
    recv(remote: HLCTimestamp): HLCTimestamp;
    /**
     * Get the current HLC state without advancing it.
     */
    peek(): HLCTimestamp;
}
/**
 * Serialize an HLC timestamp to a string.
 * Format: "wallTime-counter-nodeId"
 * Example: "1715299200000-0001-ua7x2k"
 *
 * The counter is zero-padded to 4 digits for lexicographic sorting.
 */
declare function serializeHLC(ts: HLCTimestamp): string;
/**
 * Deserialize a string back to an HLC timestamp.
 */
declare function deserializeHLC(str: string): HLCTimestamp;
/**
 * Compare two HLC timestamps.
 *
 * Returns:
 * - negative if a < b
 * - positive if a > b
 * - 0 if equal
 *
 * Comparison order: wallTime → counter → nodeId (lexicographic tie-break)
 */
declare function compareHLC(a: HLCTimestamp, b: HLCTimestamp): number;
/**
 * Compare two serialized HLC strings.
 * More efficient than deserializing both — leverages lexicographic ordering.
 */
declare function compareHLCStrings(a: string, b: string): number;
/**
 * Returns the greater of two HLC timestamps.
 */
declare function maxHLC(a: HLCTimestamp, b: HLCTimestamp): HLCTimestamp;
/**
 * Generate a short random node ID (8 chars, alphanumeric).
 */
declare function generateNodeId(): string;

/**
 * Meridian — CRDT Data Structures
 *
 * Implements Last-Writer-Wins (LWW) conflict-free replicated data types:
 * - LWWRegister: Single value with HLC timestamp
 * - LWWMap: Map of field names → LWW registers (represents one document/row)
 * - mergeLWWMaps: Deterministic field-by-field merge of two maps
 *
 * Every field in a document is an independent LWW register, enabling
 * concurrent edits to different fields without data loss.
 */
/**
 * State of a Last-Writer-Wins Register.
 * Stores a value alongside the HLC timestamp of when it was set.
 */
interface LWWRegisterState<T = unknown> {
    /** The current value */
    value: T;
    /** HLC timestamp as string (serialized) */
    hlc: string;
    /** Node ID that set this value */
    nodeId: string;
}
/**
 * Create a new LWW register state.
 */
declare function createRegister<T>(value: T, hlc: string, nodeId: string): LWWRegisterState<T>;
/**
 * Merge two LWW register states.
 * The register with the higher HLC wins.
 * On HLC tie, the higher nodeId wins (deterministic).
 *
 * @returns The winning register state
 */
declare function mergeRegisters<T>(local: LWWRegisterState<T>, remote: LWWRegisterState<T>): LWWRegisterState<T>;
/** Reserved field name for soft-delete tombstone */
declare const DELETED_FIELD = "__deleted";
/**
 * An LWW-Map represents a single document/row.
 * Each field is an independent LWW register.
 *
 * Structure:
 * {
 *   "title":      { value: "Buy milk",  hlc: "...", nodeId: "..." },
 *   "done":       { value: false,       hlc: "...", nodeId: "..." },
 *   "__deleted":  { value: false,       hlc: "...", nodeId: "..." },
 * }
 */
type LWWMap = Record<string, LWWRegisterState>;
/**
 * Create a new LWW-Map from a plain object.
 * All fields get the same initial HLC timestamp.
 */
declare function createLWWMap(fields: Record<string, unknown>, hlc: string, nodeId: string): LWWMap;
/**
 * Merge two LWW-Maps field by field.
 * Each field is independently merged using LWW register semantics.
 * Fields present in one map but not the other are included as-is.
 *
 * @returns The merged map and a list of conflicts
 */
declare function mergeLWWMaps(local: LWWMap, remote: LWWMap): MergeResult;
/**
 * Extract the plain values from an LWW-Map (strip CRDT metadata).
 * Excludes the __deleted field.
 */
declare function extractValues(map: LWWMap): Record<string, unknown>;
/**
 * Check if a document is soft-deleted.
 */
declare function isDeleted(map: LWWMap): boolean;
/**
 * Get the latest HLC string across all fields in a map.
 * Used for determining the document's "last updated" time.
 */
declare function getLatestHLC(map: LWWMap): string;
/**
 * Extract the HLC metadata map (field → hlc string).
 * Used for _meridian_meta JSONB column in PostgreSQL.
 */
declare function extractMetadata(map: LWWMap): Record<string, string>;
/**
 * Reconstruct an LWW-Map from plain values + metadata.
 * Inverse of extractValues + extractMetadata.
 */
declare function reconstructLWWMap(values: Record<string, unknown>, metadata: Record<string, string>, defaultNodeId?: string): LWWMap;
interface MergeResult {
    /** The merged LWW-Map */
    merged: LWWMap;
    /** List of field-level conflicts (for logging/debugging) */
    conflicts: ConflictRecord[];
}
interface ConflictRecord {
    /** Field name where conflict occurred */
    field: string;
    /** The value that won */
    winnerValue: unknown;
    /** HLC of the winning write */
    winnerHlc: string;
    /** Node that performed the winning write */
    winnerNodeId: string;
    /** The value that was overwritten */
    loserValue: unknown;
    /** HLC of the losing write */
    loserHlc: string;
    /** Node that performed the losing write */
    loserNodeId: string;
}

/**
 * Meridian — Sync Protocol Types
 *
 * Defines all WebSocket message types exchanged between client and server.
 * Uses a discriminated union pattern for type-safe message handling.
 */
/**
 * A single CRDT operation — the atomic unit of synchronization.
 * Represents a field-level write to a document in a collection.
 */
interface CRDTOperation {
    /** Unique operation ID (UUID) */
    id: string;
    /** Collection name (e.g., "todos", "users") */
    collection: string;
    /** Document/row ID within the collection */
    docId: string;
    /** Field name being updated */
    field: string;
    /** New value (must be JSON-serializable) */
    value: unknown;
    /** HLC timestamp string of this operation */
    hlc: string;
    /** Node ID that generated this operation */
    nodeId: string;
}
/**
 * A server-enriched change — operation + server-assigned sequence number.
 * The seqNum is monotonically increasing and used for reliable pull cursors.
 */
interface ServerChange {
    /** Server-assigned monotonic sequence number */
    seq: number;
    /** The underlying CRDT operation */
    op: CRDTOperation;
}
/** Push local changes to the server */
interface PushMessage {
    type: 'push';
    ops: CRDTOperation[];
}
/** Pull changes since a server sequence number */
interface PullMessage {
    type: 'pull';
    /** Server sequence number — only changes with seq > since are returned */
    since: number;
}
/** Subscribe to real-time changes for specific collections */
interface SubscribeMessage {
    type: 'subscribe';
    collections: string[];
    /** Optional row-level filter — only matching documents are synced */
    filter?: Record<string, Record<string, unknown>>;
}
/** Send/update ephemeral presence data */
interface PresenceSetMessage {
    type: 'presence';
    data: Record<string, unknown>;
}
/** Authenticate or refresh auth token */
interface AuthMessage {
    type: 'auth';
    token: string;
    /** Client schema version for compatibility checking */
    schemaVersion?: number;
}
type ClientMessage = PushMessage | PullMessage | SubscribeMessage | PresenceSetMessage | AuthMessage;
/** Changes from server (pull response or real-time broadcast) */
interface ChangesMessage {
    type: 'changes';
    changes: ServerChange[];
}
/** Acknowledgment of pushed operations */
interface AckMessage {
    type: 'ack';
    /** The last server sequence number assigned to pushed ops */
    lastSeq: number;
    /** IDs of acknowledged operations */
    opIds: string[];
}
/** Rejection of a specific operation */
interface RejectMessage {
    type: 'reject';
    /** The rejected operation ID */
    opId: string;
    /** Structured error code */
    code: RejectCode;
    /** Human-readable reason */
    reason: string;
}
/** Presence updates from connected peers */
interface PresenceBroadcastMessage {
    type: 'presence';
    peers: Record<string, Record<string, unknown>>;
}
/** Compaction notification — tombstones before this seqNum were purged */
interface CompactionMessage {
    type: 'compaction';
    /** Minimum available sequence number after compaction */
    minSeq: number;
}
/** Full re-sync required (e.g., after compaction gap or schema change) */
interface FullSyncRequiredMessage {
    type: 'full-sync-required';
    reason: 'compaction' | 'schema-change';
    minSeq: number;
}
/** Auth token is about to expire */
interface AuthExpiringMessage {
    type: 'auth-expiring';
    /** Seconds until token expires */
    expiresIn: number;
}
/** Auth token has expired — connection will close */
interface AuthExpiredMessage {
    type: 'auth-expired';
}
/** Auth successful */
interface AuthAckMessage {
    type: 'auth-ack';
}
/** General error */
interface ErrorMessage {
    type: 'error';
    code: string;
    message: string;
}
type ServerMessage = ChangesMessage | AckMessage | RejectMessage | PresenceBroadcastMessage | CompactionMessage | FullSyncRequiredMessage | AuthExpiringMessage | AuthExpiredMessage | AuthAckMessage | ErrorMessage;
type RejectCode = 'VALIDATION' | 'AUTH' | 'SCHEMA_MISMATCH' | 'CONFLICT' | 'RATE_LIMIT';
type ConnectionState = 'disconnected' | 'connecting' | 'authenticating' | 'connected' | 'syncing' | 'error';
/** Pending operation stored in IndexedDB until acknowledged by server */
interface PendingOp {
    /** Operation ID */
    id: string;
    /** The CRDT operation */
    op: CRDTOperation;
    /** Previous value of the field (for rollback) */
    previousValue: unknown;
    /** Previous HLC of the field (for rollback) */
    previousHlc: string | null;
    /** Status */
    status: 'pending' | 'sending' | 'acked' | 'rejected';
    /** Timestamp when the operation was created */
    createdAt: number;
    /** Number of send attempts */
    retries: number;
}
/** Tab coordination messages via BroadcastChannel */
type TabMessage = {
    type: 'leader-resign';
    tabId: string;
} | {
    type: 'claim-leader';
    tabId: string;
    lockId: number;
} | {
    type: 'leader-ack';
    tabId: string;
} | {
    type: 'leader-heartbeat';
    tabId: string;
} | {
    type: 'store-changed';
    collection: string;
    docId: string;
} | {
    type: 'request-sync';
};

/**
 * Meridian — Schema Definition System
 *
 * Lightweight, zero-dependency schema definition with:
 * - Type-safe field definitions (z.string(), z.number(), etc.)
 * - Default values for additive migrations
 * - Schema versioning
 * - TypeScript type inference from schema
 */
type FieldType = 'string' | 'number' | 'boolean' | 'array' | 'object';
interface FieldDefinition {
    type: FieldType;
    required: boolean;
    defaultValue?: unknown;
}
/**
 * Schema field helpers — inspired by Zod's API but zero-dependency.
 *
 * Usage:
 * ```ts
 * import { z } from '@meridian-sync/shared';
 *
 * const fields = {
 *   id: z.string(),
 *   count: z.number().default(0),
 *   active: z.boolean().default(true),
 * };
 * ```
 */
declare function createFieldBuilder(type: FieldType): {
    default(value: unknown): FieldDefinition;
    type: FieldType;
    required: boolean;
    defaultValue?: unknown;
};
declare const z: {
    string: () => {
        default(value: unknown): FieldDefinition;
        type: FieldType;
        required: boolean;
        defaultValue?: unknown;
    };
    number: () => {
        default(value: unknown): FieldDefinition;
        type: FieldType;
        required: boolean;
        defaultValue?: unknown;
    };
    boolean: () => {
        default(value: unknown): FieldDefinition;
        type: FieldType;
        required: boolean;
        defaultValue?: unknown;
    };
    array: () => {
        default(value: unknown): FieldDefinition;
        type: FieldType;
        required: boolean;
        defaultValue?: unknown;
    };
    object: () => {
        default(value: unknown): FieldDefinition;
        type: FieldType;
        required: boolean;
        defaultValue?: unknown;
    };
};
interface CollectionSchema {
    [fieldName: string]: FieldDefinition | ReturnType<typeof createFieldBuilder>;
}
interface SchemaDefinition {
    /** Schema version — increment on every schema change */
    version: number;
    /** Collection definitions */
    collections: Record<string, CollectionSchema>;
}
/**
 * Define a Meridian schema.
 *
 * Usage:
 * ```ts
 * const schema = defineSchema({
 *   version: 1,
 *   collections: {
 *     todos: {
 *       id: z.string(),
 *       title: z.string(),
 *       done: z.boolean().default(false),
 *       createdAt: z.number(),
 *     },
 *   },
 * });
 * ```
 */
declare function defineSchema(definition: SchemaDefinition): SchemaDefinition;
/**
 * Get the default values for a collection's fields.
 * Only includes fields that have explicit defaults.
 */
declare function getDefaults(schema: CollectionSchema): Record<string, unknown>;
/**
 * Validate a document against a collection schema.
 * Fills in default values for missing optional fields.
 *
 * @returns Normalized document with defaults applied
 */
declare function validateAndNormalize(doc: Record<string, unknown>, schema: CollectionSchema): Record<string, unknown>;
/**
 * Get all field names for a collection, including system fields.
 */
declare function getFieldNames(schema: CollectionSchema): string[];
/**
 * Map Meridian field types to PostgreSQL column types.
 */
declare function fieldTypeToSQL(type: FieldType): string;
/**
 * Infer TypeScript type from a field definition.
 * Used at compile time only.
 */
type InferFieldType<F extends FieldDefinition> = F['type'] extends 'string' ? string : F['type'] extends 'number' ? number : F['type'] extends 'boolean' ? boolean : F['type'] extends 'array' ? unknown[] : F['type'] extends 'object' ? Record<string, unknown> : unknown;
/**
 * Infer a document type from a collection schema.
 */
type InferDocument<S extends CollectionSchema> = {
    [K in keyof S]: InferFieldType<S[K] extends FieldDefinition ? S[K] : FieldDefinition>;
};

/**
 * Meridian — Permission Rules DSL
 *
 * Firebase-like security rules for collections.
 * Define who can read/write which fields, with
 * row-level access control based on auth context.
 *
 * Usage:
 * ```ts
 * const rules = defineRules({
 *   todos: {
 *     read: (auth, doc) => auth != null,
 *     write: (auth, doc) => auth?.userId === doc.ownerId,
 *   },
 * });
 * ```
 */
interface AuthContext {
    userId: string;
    role?: string;
    [key: string]: unknown;
}
interface DocAccessContext {
    /** Document being accessed (null for create/pre-query) */
    existing: Record<string, unknown> | null;
    /** Fields being written (null for read/delete) */
    incoming: Record<string, unknown> | null;
}
type RuleFn = (auth: AuthContext | null, doc: DocAccessContext) => boolean | Promise<boolean>;
interface CollectionRules {
    /** Read permission — called on find/findOne/live/subscribe */
    read?: RuleFn;
    /** Write permission — called on put/update/delete */
    write?: RuleFn;
}
interface PermissionRules {
    [collection: string]: CollectionRules;
}
/**
 * Define permission rules for collections.
 * Returns a PermissionRules object that can be passed to createServer.
 */
declare function defineRules(rules: PermissionRules): PermissionRules;
declare class RuleEvaluator {
    private rules;
    constructor(rules: PermissionRules);
    /**
     * Check if an operation is allowed on a collection.
     *
     * @param collection - Collection name
     * @param operation - 'read' or 'write'
     * @param auth - Auth context (null = unauthenticated)
     * @param doc - Document context
     * @returns true if allowed
     */
    check(collection: string, operation: 'read' | 'write', auth: AuthContext | null, doc?: DocAccessContext): Promise<boolean>;
    /**
     * Filter a list of documents to only include
     * those the user is allowed to read.
     */
    filterRead(collection: string, auth: AuthContext | null, docs: Record<string, unknown>[]): Promise<Record<string, unknown>[]>;
    /**
     * Check if a write operation is allowed.
     */
    canWrite(collection: string, auth: AuthContext | null, existing: Record<string, unknown> | null, incoming: Record<string, unknown>): Promise<boolean>;
}

/**
 * Meridian — Storage Adapter Interface
 *
 * Abstract storage interface enabling multiple database backends.
 *
 * Currently implemented:
 * - PostgreSQL (via PgStore in @meridian-sync/server)
 *
 * V2 Roadmap:
 * - SQLite (Turso / libsql)
 * - MySQL
 */

interface StorageAdapter {
    /** Initialize storage — create tables, indexes, etc. */
    init(): Promise<void>;
    /** Apply CRDT operations and return changes with sequence numbers */
    applyOperations(ops: CRDTOperation[]): Promise<{
        changes: ServerChange[];
        conflicts: ConflictInfo[];
    }>;
    /** Get all changes since a sequence number. Returns null if compaction gap. */
    getChangesSince(since: number): Promise<ServerChange[] | null>;
    /** Get the current minimum sequence number (for compaction tracking) */
    getMinSeq(): Promise<number>;
    /** Permanently delete tombstones older than the given age */
    compact(maxAgeMs: number): Promise<number>;
    /** Clean up and close connections */
    close(): Promise<void>;
}
interface ConflictInfo {
    collection: string;
    docId: string;
    field: string;
    winnerValue: unknown;
    loserValue: unknown;
}
interface StorageAdapterConfig {
    schema: SchemaDefinition;
    /** Called when data changes (for LISTEN/NOTIFY or similar) */
    onChange?: (collection: string, docId: string) => void;
    debug?: boolean;
}
interface PostgresAdapterConfig extends StorageAdapterConfig {
    connectionString: string;
}
interface SQLiteAdapterConfig extends StorageAdapterConfig {
    /** Path to SQLite database file, or ':memory:' for in-memory */
    databasePath: string;
}

/**
 * Meridian — Transport Abstraction Layer
 *
 * Meridian is transport-agnostic. The sync engine works with any
 * bidirectional message channel — not just WebSocket.
 *
 * Supported transports:
 * - WebSocket (default, browser + Node.js)
 * - WebRTC DataChannel (P2P, no server needed)
 * - TCP Socket (native apps, embedded systems)
 * - Redis Pub/Sub (server-to-server replication)
 * - NATS / Kafka (enterprise message brokers)
 *
 * Implement this interface to add a new transport.
 */

interface Transport {
    /** Send a message to the other side */
    send(msg: ClientMessage | ServerMessage): void;
    /** Called when a message is received */
    onMessage(callback: (msg: ClientMessage | ServerMessage) => void): void;
    /** Called when the connection opens */
    onOpen(callback: () => void): void;
    /** Called when the connection closes */
    onClose(callback: (code?: number, reason?: string) => void): void;
    /** Called on error */
    onError(callback: (err: Error) => void): void;
    /** Open the connection */
    connect(): Promise<void>;
    /** Close the connection */
    close(): void;
    /** Whether the transport is connected */
    readonly connected: boolean;
}
declare class WebSocketTransport implements Transport {
    private ws;
    private url;
    private _onMessage?;
    private _onOpen?;
    private _onClose?;
    private _onError?;
    constructor(url: string);
    get connected(): boolean;
    send(msg: ClientMessage | ServerMessage): void;
    onMessage(callback: (msg: ClientMessage | ServerMessage) => void): void;
    onOpen(callback: () => void): void;
    onClose(callback: (code?: number, reason?: string) => void): void;
    onError(callback: (err: Error) => void): void;
    connect(): Promise<void>;
    close(): void;
}
/** Currently supported transports */
type TransportType = 'websocket';
/** Planned transports (not yet implemented) */
type ExperimentalTransportType = 'webrtc' | 'tcp' | 'redis' | 'nats' | 'kafka';
interface TransportConfig {
    type: TransportType | ExperimentalTransportType;
    url: string;
    options?: Record<string, unknown>;
}
/** Create a transport by type. Experimental transports throw with a clear message. */
declare function createTransport(config: TransportConfig): Transport;

/**
 * Meridian — E2E Encryption Layer
 *
 * Field-level AES-256-GCM encryption for:
 * - IndexedDB at-rest encryption (browser)
 * - WebSocket in-transit encryption (second layer over TLS)
 * - Server-side blind storage (server can't read field values)
 *
 * Key derivation: PBKDF2 with 100K iterations
 * Encryption: AES-256-GCM with random 12-byte IV per operation
 */
/**
 * Derive an AES-256-GCM key from a password and salt.
 */
declare function deriveKey(password: string, salt: Uint8Array): Promise<CryptoKey>;
/**
 * Encrypt a plaintext value with AES-256-GCM.
 * Returns Uint8Array containing [IV || ciphertext].
 */
declare function encryptValue(key: CryptoKey, plaintext: string): Promise<Uint8Array>;
/**
 * Decrypt a value encrypted with encryptValue().
 */
declare function decryptValue(key: CryptoKey, packed: Uint8Array): Promise<string>;
/**
 * Encrypt specific fields in a document for blind server storage.
 *
 * ```ts
 * const encrypted = await encryptFields(doc, key, ['title', 'description']);
 * // { title: ArrayBuffer, description: ArrayBuffer, done: false }
 * ```
 */
declare function encryptFields(doc: Record<string, unknown>, key: CryptoKey, fields: string[]): Promise<Record<string, unknown>>;
/**
 * Decrypt specific fields in a document.
 */
declare function decryptFields(doc: Record<string, unknown>, key: CryptoKey, fields: string[]): Promise<Record<string, unknown>>;
/**
 * Encrypt a CRDT operation for transport over WebSocket.
 * Adds a second encryption layer on top of TLS.
 */
declare function encryptOperation(key: CryptoKey, op: {
    value: unknown;
}): Promise<{
    value: Uint8Array;
}>;
/**
 * Generate a random encryption key for demo/development.
 * In production, use a proper key management system.
 */
declare function generateRandomPassword(length?: number): string;
/**
 * Encode a Uint8Array to base64 for JSON serialization.
 */
declare function uint8ToBase64(data: Uint8Array): string;
/**
 * Decode a base64 string back to Uint8Array.
 */
declare function base64ToUint8(base64: string): Uint8Array;

export { type AckMessage, type AuthContext, type AuthExpiredMessage, type AuthExpiringMessage, type AuthMessage, type CRDTOperation, type ChangesMessage, type ClientMessage, type CollectionRules, type CollectionSchema, type CompactionMessage, type ConflictInfo, type ConflictRecord, type ConnectionState, DELETED_FIELD, type DocAccessContext, type ErrorMessage, type ExperimentalTransportType, type FieldDefinition, type FieldType, type FullSyncRequiredMessage, HLC, type HLCTimestamp, type InferDocument, type LWWMap, type LWWRegisterState, type MergeResult, type PendingOp, type PermissionRules, type PostgresAdapterConfig, type PresenceBroadcastMessage, type PresenceSetMessage, type PullMessage, type PushMessage, type RejectCode, type RejectMessage, RuleEvaluator, type RuleFn, type SQLiteAdapterConfig, type SchemaDefinition, type ServerChange, type ServerMessage, type StorageAdapter, type StorageAdapterConfig, type SubscribeMessage, type TabMessage, type Transport, type TransportConfig, type TransportType, WebSocketTransport, base64ToUint8, compareHLC, compareHLCStrings, createLWWMap, createRegister, createTransport, decryptFields, decryptValue, defineRules, defineSchema, deriveKey, deserializeHLC, encryptFields, encryptOperation, encryptValue, extractMetadata, extractValues, fieldTypeToSQL, generateNodeId, generateRandomPassword, getDefaults, getFieldNames, getLatestHLC, isDeleted, maxHLC, mergeLWWMaps, mergeRegisters, reconstructLWWMap, serializeHLC, uint8ToBase64, validateAndNormalize, z };
